 
export default class LevelScene extends Phaser.Scene{
    constructor(key : string) {
        super({
          key : key
        })
    }

    public create () { 
      console.log("hola");
    }
    
}